# Chaves para o notebook 02 (detecção de componentes via LLMs).
# Copie para .env e preencha os valores. O .env fica nesta pasta (notebooks/).
# OPENAI_API_KEY e GEMINI_API_KEY são usadas por Gemini e OpenAI.
# Qwen e Ollama rodam localmente (Docker), sem chave de API.

OPENAI_API_KEY=
GEMINI_API_KEY=
