# 20 — Design

Artefatos de arquitetura e contrato:

- **architecture.md** — Fluxo e componentes (Pipeline pluggável, DiagramAgent, StrideAgent, DreadAgent, RAG).
- **api-contracts.md** — Contrato da API (POST /api/v1/threat-model/analyze, health).
